<%# Must pass an item local variable %>
<% if item.picture_url.nil? %>
  <img data-interchange="[<%= image_url('placeholder.gif') %>, (default)]">
<% else %>
  <img data-interchange="[<%= image_url(item.picture_url) %>, (default)]">
<% end %>