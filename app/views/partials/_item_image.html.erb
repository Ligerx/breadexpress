<%# Must pass an item local variable %>
<% if item.picture_url.nil? %>
  <img data-interchange="[<%= image_tag('placeholder.gif') %>, (default)]">
<% else %>
  <img data-interchange="[<%= image_tag(item.picture_url) %>, (default)]">
<% end %>